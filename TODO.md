# TODO — 작업 목록

작업 관리 보드. 상태는 `[ ]` 대기 · `[~]` 진행중 · `[x]` 완료.
완료된 로드맵은 아래 "완료" 섹션에 보관하고, 위쪽에서 현재/다음 작업을 관리한다.

---

## 🔥 지금 (In Progress)

- [ ] <현재 잡고 있는 작업 — 하나만 두는 걸 권장>

## ⏭️ 다음 (Next — 우선순위 순)

- [x] **승리 금액 표시 버그 2종: 패자 +표시·총액 표시 → 순수익 기준** — payouts 가
      "팟에서 받아간 총액"(본인 베팅 반환+언콜드 환급 포함)이라 ①올인에서 진 쪽도
      환급이 +금액·승자 표시로 보이고 ②승자도 실제 딴 돈이 아닌 총액(+올인 금액)이 표시됨.
      수정: `HandEngine.netResults()`(획득 총액-넣은 칩, 환급=0·패자=음수) 추가,
      `TableStateView.netResults` 로 노출, 프론트 승자 마크·+금액·🏆 배너·승리 효과음을
      전부 순수익 기준으로 전환(payouts 는 팟 분배 원장으로 유지). 엔진 회귀 테스트 1건
      (전체 160개 통과), 프론트 빌드 통과. **서버 재시작 필요**
- [x] **쇼다운 족보 라벨(누가 왜 이겼는지)** — 홀카드가 공개된 좌석의 카드 위에
      "투 페어 A·9"/"플러시 (A 하이)" 등 한국어 족보 배지 표시(승자는 🏆+골드 강조).
      서버: `SeatView.handLabel` + `table/HandLabels`(HandRank→한국어, 리댁션과 동일 기준이라
      미공개 좌석은 항상 null — 정보 유출 없음). 본인 좌석은 플랍부터 실시간 표시,
      리플레이 프레임에도 표기. 올인 런아웃 중엔 숨겨 리버 전 스포일러 방지.
      테스트 4건 추가(전체 159개 통과), 프론트 빌드 통과. **서버 재시작 필요**

- [x] **리바인(버스트 후 즉시 재구매) + 횟수 한도 인프라(미적용)** — 버스트 시 10분 쿨다운
      때문에 다시 앉을 수 없던 것을 리바인 경로로 해소: `TableService.rebuy` +
      STOMP `/app/table/{id}/rebuy`(착석 중엔 거부, principal 신뢰), RuleGuard
      `checkAndRecordRebuy`(쿨다운 면제·횟수 기록·bustAt 해소). 한도는
      `BuyInPolicy.maxRebuys`(0=무제한 — **기능은 준비, 현재 미적용**, 4-인자 호환 생성자 유지).
      프론트: 버스트 시 "💸 칩을 모두 잃었습니다" 박스(금액 입력+리바인 버튼, 완료 후
      "다음 핸드부터 참여" 안내). 테스트 5건 추가 + 실서버 E2E(버스트 1.2초 뒤 리바인 →
      즉시 재착석·에러 없음)
- [x] **올인 런아웃 연출 전면 개편(생동감)** — 시간차만 있던 공개를 실제 포커사이트식
      임팩트로 재구성: ①스트리트 공개 순간 테이블 퀘이크(화면 흔들림 0.6초)
      ②펠트 전체 골드 플래시(radial) ③런아웃 내내 심장박동 비네트(붉은 어둠 맥동)
      ④방금 공개된 카드 대형 3D 플립(1.7배 스케일→안착) + 골드 글로우(플랍은 3장 시차)
      ⑤배너에 플랍→턴→리버 진행 스텝(현재 단계 펄스) ⑥전용 효과음(턴=심장 쿵쿵,
      리버=상승 리저+쿵) ⑦서스펜스 타이밍(플랍 1.2s→턴 +1.6s→리버 +1.9s→결과 +1.5s,
      합계 6.2s < 서버 쇼다운 추가 대기 7.5s). `useAllInRunout` 이 phase 메타 노출하도록 재구성
- [x] **올인 동결 진짜 원인: 버스트 좌석 제거 → 종료 브로드캐스트 누락** — 최신 빌드에서도
      재보고돼 살아있는 서버 스레드 덤프로 진단: 락·데드락·계산멈춤 전부 아님, 서버는 정상 진행.
      실체는 "올인에서 진" 플레이어가 `settleBustsIfHandComplete` 로 좌석이 즉시 비워진 뒤
      `ViewBroadcaster` 가 착석자에게만 전송 → 패자는 종료 화면을 영영 못 받아 클라이언트가
      "AI 생각 중"+타이머 프레임에 고착(서버는 핸드가 이미 끝나 자동폴드할 것도 없음).
      이전 E2E 가 통과한 이유 = kim 이 올인에서 이겨 좌석 유지. 수정: 브로드캐스트 대상을
      "착석자 ∪ 현재 핸드 참가자"(`TableService.broadcastTargetIds`)로 확장.
      검증: 회귀 테스트 1건(버스트 후에도 대상 포함+종료 뷰 수신 가능, 전체 155개 통과) +
      실서버 E2E(kim 400 매핸드 올인 → 핸드 2에서 패배·버스트 → 최종 payouts 화면 정상 수신)
- [x] **bts-preflop.json 재전사(이전 대체)** — 이전 PC에서 가져올 수 없어 원본 PDF
      (`C:\preflop\BTS_프리플랍바이블+v2.1`)를 이 PC에서 재판독. PyMuPDF 픽셀 판독 자동화
      (그리드 이미지 13x13 분할 + 실측 앵커색 분류 + 분할셀 비율 스냅)로 차트 42개 전사,
      인쇄된 콤보 수와 대조 검산(다수 완전 일치, 최대 오차 ±0.3%p). 판독 스크립트는
      `C:\preflop\extract.py` 백업(재생성 가능). 스모크 테스트 2건 실행·통과
- [x] **해링턴 캐시게임 전략 봇 적용(포스트플랍)** — `bot/PostflopAdvisor`: ①핸드 강도
      5분류(괴물/아주 강함/강함/미디엄/드로우+에어, 콤보 드로우 플래그) ②플랍 텍스처
      4분류(마른/젖은/페어드/A하이)와 c-bet 보드 선별 ③베팅 사이즈 규칙(마른 1/2팟·
      밸류 2/3·젖은 팟, 멀티웨이 한 단계 크게) ④팟 컨트롤(원페어 턴 감속·리버 씬 밸류
      1/2팟·리버 레이즈 자제) ⑤경보 규칙(내 벳에 레이즈 → 원페어 폴드) ⑥드로우 배당
      콜(플랍 룰오브4)·콤보 드로우 세미블러프 레이즈 ⑦멀티웨이 블러프 금지.
      BotBrain: 프리플랍=BTS 차트, 포스트플랍=해링턴 규칙, 규칙 밖=이퀴티 폴백.
      혼합 빈도 `poker.bot.cbet-freq`(0.7)/`poker.bot.steal-freq`(0.4). 테스트 14건
- [x] **상대 모델링(StatsService 확장) + 유형별 착취** — `stats/HandLogTally`(종료 핸드
      로그 1회 재생으로 AF/WtSD/F3B 원료 집계), PlayerStats·스냅샷 확장(구버전 파일 호환),
      PostflopAdvisor 유형 판별(30핸드+): 콜스테이션(VPIP>35·AF<1.2)=블러프 금지,
      니트(VPIP<16)=C-벳 100%·턴/리버 베팅엔 원페어 폴드, LAG(VPIP>25·PFR>18·AF>2.5)=
      레이즈 역해석 콜다운·플랍 트랩. 테스트 5건 추가
- [x] **봇 소극성 버그 3종 수정(poker_ai.png 사례)** — 리버 톱페어 체크 문제의 근본 원인:
      ①리버 원페어 규칙이 상대 "라인"을 무시 → `Hist.postflopBetSeen` 추가, 모두 체크로 온
      팟이면 스케어 보드/멀티웨이라도 1/2팟 밸류벳(교재: "겁먹은 체크는 대형 실수")
      ②이퀴티 폴백 밸류벳 기준이 고정 62% → 공평 지분 비례(1.24/인원수, 헤즈업 62% 동일)로
      변경 — 풀테이블에서 AA도 영원히 벳 못 하던 림프-체크 파티 해소
      ③프리플랍 차트가 2~6인 전용 → 7~9인은 늘어난 얼리 좌석을 UTG 레인지로 매핑해 차트 유지.
      ④(후속) 리버 밸류벳 100% 고정은 착취 가능 → `poker.bot.river-value-freq`(기본 0.8)
      빈도 혼합으로 가끔 체크해 범위 위장. 재현·회귀 테스트 5건 추가(전체 140개 통과)
- [x] **프리플랍 미오픈(A4s/KJs/QTo) 원인 확인 + 회귀 고정** — poker_ai_2~4 스크린샷은
      7~9인 차트 확장(a4eb2d1) 이전 빌드의 동작(차트 꺼짐 → 이퀴티 폴백이 전부 폴드).
      BTS 차트는 UTG 조차 A4s 오픈 1.0 이므로 새 빌드에선 정상 오픈 —
      9인 UTG A4s 차트 오픈 스모크 테스트로 회귀 고정. **서버 재빌드·재시작 필요**
- [x] **PokerNow 스타일 프론트 개선** — ①타이머를 숫자 링 → 줄어드는 바(TimerBar,
      1초 선형 감소·5초 이하 빨강 펄스, 좌석 아래+턴 힌트) ②좌석 액션 말풍선
      (SeatView.lastAction: 이번 스트리트 마지막 액션 "체크/콜/벳 60/레이즈 60",
      벳·레이즈는 골드, 새 스트리트가 깔리면 사라짐 — 이벤트 소싱 재생으로 계산)
- [x] **동결 구조적 차단: 봇 판단을 테이블 락 밖으로(스크린샷 56)** — 프리플랍 올인에 또
      "AI 생각 중" 동결 보고. 근본 결함: `actIfBotTurn` 이 테이블 락을 쥔 채 `brain.decide`
      (몬테카를로 포함)를 실행 → 판단이 어디서든 멈추면 타임아웃 자동폴드(enforceTimeout)도
      같은 락에 블록돼 스케줄러 풀을 늘려도 동결. 수정: ①판단은 락 밖 전용 스레드(풀 2)에서
      `HandLog` 스냅샷 복제 엔진으로 실행 + 제한시간 `poker.bot.decide-timeout-ms`(기본 5초)
      초과 시 안전 액션(체크/폴드) 강제 ②적용 전 액션 수 비교로 낡은 판단 폐기.
      → 이제 판단이 예외·무한대기여도 최악 30초(타임뱅크)에 자동폴드+브로드캐스트가 보장됨.
      검증: 락 프리 회귀 테스트 2건 + 실서버 E2E(STOMP로 kim 프리플랍 올인 → 봇 1.4초 내
      이퀴티 콜 "이퀴티 49% ≥ 필요 49%" → 핸드 종료 브로드캐스트, 동결 없음. 스크린샷 56은
      수정 전 빌드로 추정 — **재빌드·재시작 필수**). 전체 154개 테스트 통과
- [x] **올인 런아웃 연출(두근두근)** — 올인으로 남은 보드가 한 프레임에 전부 깔리던 것을
      프론트 `useAllInRunout` 훅으로 "홀카드 공개 → 플랍 → 턴 → 리버 → 결과" 시차 공개
      (결과 프레임까지 스택·팟 분배·승자 보류, 카드별 딜 효과음은 기존 SFX 재사용,
      "🃏 올인 쇼다운" 금색 펄스 배너). 서버는 쇼다운 종료 시 자동진행 추가 대기
      `poker.autodeal.showdown-extra-ms`(기본 3.5초)로 연출이 잘리지 않게 지원.
      백엔드 테스트 1건 추가(152개 통과), 프론트 빌드 통과
- [x] **복기 "내 실수만" + 무지성 올인 응징** — ①복기 패널이 전 좌석(AI 포함)의 실수를
      배너로 띄워 "AI가 스스로 실수 분석"처럼 보임 → 최대 실수·마커·배너를 보는 사람(viewerId)
      기준으로 필터(세션 리포트 표는 플레이어별 라벨이라 유지) ②아무 패 올인에 봇이 전부 폴드:
      프리플랍 3벳/폴드 차트는 상대 올인 시 레이즈 불가라 AA 조차 폴드, 해링턴 "리버 큰 벳 =
      원페어 폴드"도 올인에 100% 적용돼 착취 가능 → `BotBrain.facingEffectiveAllIn`(콜하면 내가
      올인 or 현재 벳 주인이 올인)이면 차트·규칙 우회, 이퀴티 vs 팟오즈 폴백으로 콜/폴드
      (상대 랜덤 가정은 무지성 올인일수록 정확 → 자동 응징, 쓰레기 핸드는 여전히 폴드).
      테스트 4건 추가(전체 151개 통과), 프론트 빌드 통과. **서버 재시작 필요**
- [x] **AI 올인 대치 동결 방어 + 헤즈업 오픈 빈도 보정(스크린샷 55)** — 리버에서 사람이
      봇의 벳에 올인 레이즈하자 "AI 생각 중"으로 영구 멈춤(타임아웃 자동폴드·자동진행도 무반응).
      단일 원인 재현은 실패(엔진·봇·뷰 경로 150판 시뮬 통과 → BotRiverAllInFreezeTest 로 고정),
      멈춤을 만들 수 있는 구조 결함 4개를 전부 수정:
      ①타임아웃 자동액션이 브로드캐스트 없이 적용돼 클라이언트가 이전 화면에 멈춰 보임
      → TurnTimeoutSweeper 를 web 으로 이동(의존 방향 준수) + 액션 후 전파
      ②스위퍼 3개(봇/타임아웃/자동진행)가 스케줄러 단일 스레드 공유 — 하나만 막혀도 전부 동결
      → `spring.task.scheduling.pool.size=4`
      ③봇 두뇌 예외·불법 액션 시 무한 "생각 중" 루프 → 안전 액션(체크/폴드) 폴백 + 에러 로그
      ④스위퍼 예외를 무로그로 삼켜 진단 불가 → warn 로그.
      헤즈업 오픈: 6-max SB 차트 그대로 써서 오픈 49%(K6o↓/Q6o↓/J6o↓ 전부 폴드) → 헤즈업이면
      경계선 전량 오픈 승격 + 차트 밖 핸드 `poker.bot.hu-open-boost`(0.5) 빈도 혼합 ≈ 78% 오픈
      (특정 핸드 무조건 오픈이 아닌 빈도 보정). 테스트 6건 추가(전체 147개 통과). **서버 재시작 필요**
- [x] **초보자용 용어 설명(프론트)** — 용어 사전 GLOSSARY(~55개) + 클릭형 `<Term>`/`<JargonText>`:
      ①복기 최대 실수·현재 실수 배너 아래 "왜 실수인가요?" 부연설명(오버콜/오버폴드를 문장으로 풀이)
      ②AI 판단 로그·복기 헤더·판정 전제·리더보드(VPIP/PFR/net)·세션 리포트의 용어
      (필요이퀴티·레이즈마진·씬 밸류 등)를 점선 밑줄 → 클릭 시 인라인 설명 펼침
      ③테이블 "내 승률 %" 배지 클릭 → 몬테카를로 계산 근거 팝오버(내 카드·보드·상대 수 반영,
      "상대 랜덤 가정"의 한계 고지). 백엔드 무변경, 프론트 빌드 통과

## 💡 아이디어 (Backlog — 확정 안 됨)

- [x] 프리플랍 레인지 오버레이 — 정적 JSON(6-max 100bb RFI), `GET /api/preflop-range`, 히어로 핸드 오픈 판정
- [x] 3인 이상 테이블 UI — 좌석 동적 렌더(flex-wrap)로 2~6인 지원, 타임뱅크 카운트다운 표시
- [x] 관전 모드 — `GET /api/tables/{id}/spectate`, 착석 없이 구경(홀카드 전부 숨김)
- [~] 배포: AWS Lightsail(2GB) — **준비 완료**(Dockerfile·prod 프로파일·이퀴티 반복 외부화·DEPLOY.md).
      실제 라이브 배포 실행만 남음(AWS 계정·SSH 자격증명 필요 → 사용자가 [DEPLOY.md](DEPLOY.md) 따라 실행)

---

## ✅ 완료 (로드맵)

- [x] 게임 엔진 도메인 + 단위테스트 (콘솔/테스트로 한 판 끝까지) — `HandEngine`, `eval`, 300판 칩보존 속성 테스트
- [x] STOMP 연동 + React 테이블 UI (2인용) — 개인화 리댁션 뷰, WebSocket 통합테스트
- [x] RuleGuard — 최소 바이인 / 재입장 쿨다운 / 일일 리로드 한도
- [x] 이퀴티 오버레이 — 몬테카를로, 본인 이퀴티만 push
- [x] StatsService + ROI 리더보드 — VPIP/PFR/net, `GET /api/leaderboard`
- [x] 핸드 히스토리 리플레이 — 이벤트 소싱(`HandLog`)으로 지난 판 프레임 단위 되감기, `GET /api/tables/{id}/hands`
- [x] 멀티 테이블 / 로비 — `GET /api/tables`로 활성 테이블 목록(좌석수·진행상태)
- [x] 타임뱅크 — 액션 30초 제한, 초과 시 자동 체크/폴드(`TurnTimer`+스케줄러), 뷰에 남은 초 노출
- [x] 통계 영속화 — JSON 파일 스냅샷(`StatsStore`/`JsonFileStatsStore`), 재시작 후 리더보드 복원
- [x] 핸드 자동 복기(EV 손실 기반 실수 감지) — `review` 패키지(`HandReviewer`), 이퀴티 vs 팟오즈로
      콜/폴드 지점 EV 손실 수치화 + 핸드당 최대 실수 1개. `GET /api/tables/{id}/hands/{index}/review`,
      세션 누적 `GET /api/tables/{id}/review/session`, 프론트 복기 패널(리플레이 되감기 + 실수 마커)
- [x] 검증 가능한 셔플(commit-reveal) — `fairness` 패키지(`CommittedShuffle`), 딜 전 SHA-256 커밋 공개 →
      종료 후 솔트+덱 리빌. `GET /api/tables/{id}/fairness`, 테이블 커밋 배지 + 복기 패널에서
      브라우저(crypto.subtle) 해시 재계산 검증. 진행 중 핸드의 덱은 절대 리빌하지 않음
- [x] AI 상대 — `bot` 패키지(`BotBrain`: 이퀴티 vs 팟오즈 판단 = 복기와 동일 철학, `BotService`+스위퍼).
      `POST/DELETE /api/tables/{id}/bots`, 봇 차례 자동 액션(생각 지연 `poker.bot.think-ms`, 기본 900ms),
      프론트 "🤖 AI 상대 추가" 버튼. 사람 혼자 + AI 로 실전 연습 가능
- [x] 전지적 관찰자 시점 — `GET /api/tables/{id}/godview`(폴드 포함 전 홀카드 공개).
      내가 플레이 중이 아닐 때(폴드/미착석/핸드 종료)만 "👁 상대 패 보기" 버튼 노출, 명시적 토글로만 공개
- [x] 베팅금액 수동 입력 수정 — 입력칸이 최소 금액으로 강제 복원돼 수정 불가능하던 버그.
      빈칸 허용 + placeholder 안내, 전송 시에만 빈 값을 최소 레이즈로 대체, 버튼에 전송 금액 표시
- [x] 자동 다음 핸드 진행 — `AutoDealService`(+스위퍼): 핸드 종료 후 딜레이(기본 4초,
      `poker.autodeal.delay-ms`) 뒤 자동 시작, 칩 보유 2명 미만이면(버스트) 중단, 첫 핸드는 수동.
      `GET/PUT /api/tables/{id}/autodeal` + 프론트 "▶ 자동진행 ON/OFF" 토글
- [x] 봇 행동 이유 표시 — `BotBrain.Decision`에 reason(예: "이퀴티 63% ≫ 필요 8% + 마진 → 팟 레이즈"),
      `GET /api/tables/{id}/bots/reasons`(진행 중 핸드는 기본 숨김, god=true 는 godview 와 동일 신뢰),
      프론트 "🧠 AI 판단 로그" 패널
- [x] 홈(랜딩) 페이지 분리 — 첫 접속 시 홈(타이틀·기능 소개·착석 폼·저장된 플레이어·테이블 로비),
      착석하면 자동 입장, 테이블 헤더 "🏠 홈" 버튼으로 왕복(연결·게임 상태 유지)
- [x] 온라인 포커사이트 스타일 UI 리디자인 — 다크 크롬+골드 디자인 시스템, 우드 레일·베팅 라인·
      펠트 워터마크 테이블, 해시 색상 아바타 좌석, 도미네이션 칩스택, 대형 액션바 + 베팅 슬라이더
      (½팟·⅔팟·팟·올인 퀵버튼), 히어로 랜딩(기능 카드 6종·로비), 스트리트별 배지 색,
      승리 금액 플로팅, WebAudio 합성 효과음(내 차례·딜·칩·승리, 🔊 토글) — 파일 의존성 0
- [x] 봇 프리플랍 차트 연동 — 60p 프리플랍 교재(개인 소장 PDF)를 픽셀 판독으로 전사(차트 43개,
      각 차트의 인쇄된 콤보 수로 전량 검산). `range/BtsPreflopCharts`(JSON 없으면 자동 비활성) +
      `bot/PreflopAdvisor`(레이즈 수로 오픈/3벳/BB방어/스퀴즈/4벳/5벳 판별, 포지션 자동 배정,
      경계선 핸드는 `poker.bot.borderline-freq` 빈도 혼합, 셀 분할 = 콤보 비율 확률 실행).
      차트 밖 상황·포스트플랍은 기존 이퀴티 폴백. AI 판단 로그에 "차트: ..." 근거 표기.
      ⚠ 전사 JSON·요약 문서는 저작권상 gitignore(로컬 + C:\preflop 백업만)

> 현재 전체 135개 단위·통합 테스트 통과(차트 스모크는 로컬 전용 — 파일 없으면 자동 스킵).
> 배포 준비물(Docker/prod/DEPLOY.md) 완비.

---

## 📌 작업 항목 템플릿 (새 항목 복사용)

```
- [ ] **<작업명>** — <한 줄 목적>
      배치: <패키지>  |  완료기준: <테스트/문서>  |  참고: PROMPTS.md
```
